<div class="wrapper_with_padding">
  <div id="docs" class="clearfix">
    <% unless @docs.blank? %>
      <% @docs.each do |doc|  %>
        <a href="<%= (url_for [doc]) %>">
          <div class="doc">
            <h2 class="title"><%= doc.title %></h2>
            <p class="date"><%= time_ago_in_words(doc.created_at) %></p>
            <p class="content"><%= truncate(doc.content, length:50) %></p>
          </div>
        <% end %>
      </a>
    <% else%>
      <h2>Create Doc!</h2>
      <p>Start Creating Documents and Organizing</p>
      <button>
        <%= link_to "Create Doc", new_doc_path %>
      </button>
    <% end %>
  </div>
</div>
